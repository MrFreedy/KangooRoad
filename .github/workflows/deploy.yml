name: Deploy KangooRoad

on:
  push:
    branches:
      - master

jobs:

  update_code:
    runs-on: ubuntu-latest
    steps:
      - name: Update code on server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_PASSWORD }}
          script: |
            cd /home/KangooRoad/
            git fetch origin
            git reset --hard origin/master

  setup_environment:
    runs-on: ubuntu-latest
    needs: update_code
    steps:
      - name: Create .env files
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_PASSWORD }}
          script: |
            echo "POSTGRES_USER=${{ secrets.POSTGRES_USER }}" > /home/KangooRoad/.env
            echo "POSTGRES_PASSWORD=${{ secrets.POSTGRES_PASSWORD }}" >> /home/KangooRoad/.env
            echo "POSTGRES_DB=${{ secrets.POSTGRES_DB }}" >> /home/KangooRoad/.env
            echo "COOKIE_SECRET=${{ secrets.COOKIE_SECRET }}" >> /home/KangooRoad/backend/.env
            echo "DB_HOST=${{ secrets.DB_HOST }}" >> /home/KangooRoad/backend/.env
            echo "DB_PORT=${{ secrets.DB_PORT }}" >> /home/KangooRoad/backend/.env
            echo "DB_USER=${{ secrets.DB_USER }}" >> /home/KangooRoad/backend/.env
            echo "DB_PASSWORD=${{ secrets.DB_PASSWORD }}" >> /home/KangooRoad/backend/.env
            echo "DB_NAME=${{ secrets.DB_NAME }}" >> /home/KangooRoad/backend/.env


  deploy:
      runs-on: ubuntu-latest
      needs: setup_environment
      steps:
        - name: Checkout code
          uses: actions/checkout@v4

        - name: Set up Docker Buildx
          uses: docker/setup-buildx-action@v2

        - name: Deploy KangooRoad
          uses: appleboy/ssh-action@master
          with:
            host: ${{ secrets.SERVER_HOST }}
            username: ${{ secrets.SERVER_USER }}
            password: ${{ secrets.SERVER_PASSWORD }}
            script: |
              cd /home/KangooRoad/
              docker create network kangooroad-network || true
              docker-compose down --remove-orphans
              docker-compose up -d --build
